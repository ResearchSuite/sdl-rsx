<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="edu.cornell.tech.foundry.sdlrsx_example">

    <application
        android:name=".SDLRSXApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.SDLRSXExample">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name="org.researchstack.backbone.ui.ViewTaskActivity"
            android:windowSoftInputMode="adjustResize"
            android:theme="@style/Theme.SDLRSXExample.Survey"
            />

        <activity
            android:name="org.researchstack.backbone.ui.ViewWebDocumentActivity"
            android:label="@string/app_name"
            android:theme="@style/Theme.SDLRSXExample.Survey"
            />

    </application>

</manifest>